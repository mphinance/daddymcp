# --- TraderDaddy Pro data ---
# Your own td_live_ key. LEAVE BLANK to run the examples in keyless demo mode
# (typed fixtures). Copy this file to `.env` and fill it in to hit live data.
# The key stays on this host and only talks to api.traderdaddy.pro.
TD_API_KEY=
